/**  Responsive controller to change DrawObject positions and recall drawing
 * 
 * All method/attribute names initializing with _ are for internal use of the class only
 */
class InteractiveObject extends DrawObject {
  constructor(render, pos_x, pos_y){
    super(render, pos_x, pos_y);
  }

  
}